package com.lets.platform.model.psi.enums;

import cn.hutool.core.util.StrUtil;
import com.lets.platform.common.pojo.enums.IEnum;
import org.springframework.util.StringUtils;

import java.util.ArrayList;
import java.util.List;

/**
 * 销售取价方案对应单据枚举
 * @author zhangweiyan
 * @date 2023年11月18日09:44:01
 */
public enum SaleSettingCorrespondingDocEnum implements IEnum<Object> {
    SALE_ORDER("1", "销售订单"),
    SALE_QUOTATION("2", "销售报价单"),
    SALE_PRICE_LIST("3", "销售价目表"),
    CONTRACT_MANAGE("4", "合同管理"),
    ;

    private final String value;

    private final String name;

    SaleSettingCorrespondingDocEnum(String value, String name) {
        this.value = value;
        this.name = name;
    }

    public static String getValue(String name) {
        if (StringUtils.isEmpty(name)) {
            return null;
        }
        for (SaleSettingCorrespondingDocEnum element : values()) {
            if (name.equals(element.getName())) {
                return element.getValue();
            }
        }
        return null;
    }

    public static String getName(String value) {
        if (StringUtils.isEmpty(value)) {
            return null;
        }
        for (SaleSettingCorrespondingDocEnum element : values()) {
            if (value.equals(element.getValue())) {
                return element.getName();
            }
        }
        return null;
    }

    public static List<String> getValues() {
        List<String> values = new ArrayList<>();
        for (SaleSettingCorrespondingDocEnum element : values()) {
            values.add(element.getValue());
        }
        return values;
    }

    public static boolean containsValue(String value) {
        if (StrUtil.isEmpty(value)) {
            return false;
        }
        for (SaleSettingCorrespondingDocEnum element : SaleSettingCorrespondingDocEnum.values()) {
            if (value.equals(element.getValue())) {
                return true;
            }
        }
        return false;
    }

    public static boolean containsName(String name) {
        if (StrUtil.isEmpty(name)) {
            return false;
        }
        for (SaleSettingCorrespondingDocEnum element : values()) {
            if (name.equals(element.getName())) {
                return true;
            }
        }
        return false;
    }

    @Override
    public String getValue() {
        return value;
    }

    public String getName() {
        return name;
    }
}
