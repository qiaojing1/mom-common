package com.lets.platform.model.psi.enums;

import cn.hutool.core.util.StrUtil;
import com.lets.platform.common.pojo.enums.IEnum;
import org.springframework.util.StringUtils;

import java.util.ArrayList;
import java.util.List;

/**
 * 库存 选择查询方式
 * @author DING WEI
 * @date 2023/9/7 15:17
 * @version 1.0
 */
public enum StoreMaterialChooseQueryTypeEnum implements IEnum<Object> {

    IGNORE_BATCH_NUMBER("1", "忽略批号"),
    IGNORE_MATERIAL_SPECIFIC("2", "忽略特征属性"),
    SHOW_ZERO("3", "显示0库存"),
    IGNORE_STORE("6", "忽略仓库"),
    ;

    private final String value;

    private final String name;

    StoreMaterialChooseQueryTypeEnum(String value, String name) {
        this.value = value;
        this.name = name;
    }

    public static String getValue(String name) {
        if (StringUtils.isEmpty(name)) {
            return null;
        }
        for (StoreMaterialChooseQueryTypeEnum element : values()) {
            if (name.equals(element.getName())) {
                return element.getValue();
            }
        }
        return null;
    }

    public static String getName(String value) {
        if (StringUtils.isEmpty(value)) {
            return null;
        }
        for (StoreMaterialChooseQueryTypeEnum element : values()) {
            if (value.equals(element.getValue())) {
                return element.getName();
            }
        }
        return null;
    }

    public static List<String> getValues() {
        List<String> values = new ArrayList<>();
        for (StoreMaterialChooseQueryTypeEnum element : values()) {
            values.add(element.getValue());
        }
        return values;
    }

    public static List<String> getNames() {
        List<String> names = new ArrayList<>();
        for (StoreMaterialChooseQueryTypeEnum element : values()) {
            names.add(element.getName());
        }
        return names;
    }

    public static boolean containsValue(String value) {
        if (StrUtil.isEmpty(value)) {
            return false;
        }
        for (StoreMaterialChooseQueryTypeEnum element : StoreMaterialChooseQueryTypeEnum.values()) {
            if (value.equals(element.getValue())) {
                return true;
            }
        }
        return false;
    }

    public static boolean containsName(String name) {
        if (StrUtil.isEmpty(name)) {
            return false;
        }
        for (StoreMaterialChooseQueryTypeEnum element : values()) {
            if (name.equals(element.getName())) {
                return true;
            }
        }
        return false;
    }

    @Override
    public String getValue() {
        return value;
    }

    public String getName() {
        return name;
    }
}
